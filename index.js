import 'dotenv/config';  // Automatically loads and configures dotenv
import 'babel-register'; // Allows you to use Babel to transpile ES6 on the fly
import './server.js';       // Imports and runs the server
